// Smooth scrolling
function scrollToSection(sectionId) {
  document.getElementById(sectionId).scrollIntoView({ behavior: 'smooth' });
}

// Play sound effect
function playCandySound() {
  document.getElementById('candy-sound').play();
}
